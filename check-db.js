const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const svcs = await prisma.internalService.findMany({ where: { name: { contains: 'telegram', mode: 'insensitive' } }, select: { id: true, name: true, platform: true } }); console.log('Telegram Internal:', svcs.filter(s => s.platform === 'OTHER').length, 'of', svcs.length, 'are OTHER'); const psvcs = await prisma.providerService.findMany({ where: { name: { contains: 'telegram', mode: 'insensitive' } }, select: { id: true, name: true, platform: true } }); console.log('Telegram Provider:', psvcs.filter(s => s.platform === 'OTHER').length, 'of', psvcs.length, 'are OTHER'); } main().finally(() => prisma.\$disconnect());
